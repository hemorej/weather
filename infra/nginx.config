# FORGE CONFIG (DO NOT REMOVE!)
include forge-conf/3266687/before/*;
include forge-conf/3266687/weather.jerome-arfouche.ca/before/*;

server {
    http2 on;
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name weather.jerome-arfouche.ca;
    server_tokens off;
    root /home/forge/weather.jerome-arfouche.ca/current/.output/public;

    # FORGE SSL (DO NOT REMOVE!)
    ssl_certificate /etc/nginx/ssl/domains/1768515/3566921/server.crt;
    ssl_certificate_key /etc/nginx/ssl/domains/1768515/3566921/server.key;

    # 20 req/min per IP, generous burst below so normal page loads (which fetch
    # many /_nuxt/ chunks at once) aren't affected — this exists to blunt sustained
    # abuse of /api/* (each call fires several billed OWM requests), not to
    # throttle legitimate browsing. Zone declared centrally in
    # ~/Code/scripts/infra/nginx/conf.d/rate-limits.conf (weather is the only
    # app with this enabled so far).
    limit_req zone=weather_api burst=60 nodelay;
    limit_req_status 429;

    # --- Bot / scanner mitigation ---
    # Shared rules (scanner UA blocklist, WP/Joomla/IoT/admin probes, dotfile
    # drop) — canonical source: ~/Code/scripts/infra/nginx/snippets/bot-mitigation.conf
    include snippets/bot-mitigation.conf;
    # --- End bot / scanner mitigation ---

    # Serve Nuxt hashed JS/CSS chunks — immutable since filenames change on each build
    location /_nuxt/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000, immutable";
        gzip_static on;
        # brotli_static on;  # uncomment if nginx brotli module is installed
    }

    location /fonts/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000, immutable";
        gzip_static on;
        # brotli_static on;
    }

    include forge-conf/3266687/site.conf;
}

# FORGE CONFIG (DO NOT REMOVE!)
include forge-conf/3266687/after/*;
include forge-conf/3266687/weather.jerome-arfouche.ca/after/*;
